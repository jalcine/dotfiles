# vim: set ft=tmux tw=78:
# If there's nothing more important in tmux, it's your freaking statusbar.
# It's what you look at to check on things and when configured to the max, it
# can *sometimes* be the reason why you go fullscreen on your terminal
# sessions!
# TODO: Using #{pane_current_path}, can we do something with git?

# Update the status bar once every two seconds. Instead of doing it every
# second drops CPU usage (not tmux itself but the commands run when it
# refreshes).
set -gw status-interval 1

# Turn off to make rainbarf play nice on OSX.
set -gw status-utf8 on

# Make it sit in the middle.
set -gw status-justify centre

# Breathe, tmux.
set -gw visual-activity on
set -gw visual-bell on
set -gw monitor-activity on
set -gw bell-action current
set -gw bell-on-alert on

set -gw status-bg black
set -gw status-fg white
set -gw status-left-length 120
set -gw status-right-length 120

# TODO Move this into my shell theme, make a fallback.
#set -gw status-left  '#(jalcine_tmux_prompt --left)'
#set -gw status-right '#(jalcine_tmux_prompt --right)'
#set -gw status-left '#[fg=colour255,bg=colour27] ✍ #S:#(hostname) #[bg=colour253,fg=colour253]#[bg=default,fg=colour253]'
#set -gw status-right "#[fg=colour231,bg=colour233,bold] ✪ #(date +'%m/%d/%g %T') #[default]"
set -gw status-left "#[bg=white,fg=black] #S#[bold]@#[nobold]#[underscore]#(hostname)#[nounderscore] #[default]"
set -gw status-right "#[bold] ✪ #(date +'%m/%d/%g %T') #[default]"
